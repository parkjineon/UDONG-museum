import styled from "styled-components";

const COMMON_MARGIN = "120px";
const fontSizes = {};

const colors = {
  // main: "#1f2747",
  main: "#5477F0",
  error: "#F13838",
};

const theme = {
  COMMON_MARGIN,
  fontSizes,
  colors,
};

export default theme;
